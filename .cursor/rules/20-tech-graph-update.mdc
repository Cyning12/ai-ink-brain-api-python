## 图谱自动增量更新规则（核心）

### 触发条件
- 当代码、表结构、接口、页面、逻辑发生变动时：必须自动更新 `docs/_tech_graph/` 中对应图谱文件。
- 禁止全量重新生成。

### 更新原则
- 只改受影响的节点/流程，不破坏其他内容。
- 版本时间线 `02_version.md` 自动追加版本记录。
- 保持图谱结构稳定，不随意变更文件名称。

### AI 必须遵守
- 每次修改代码前，先读取对应 `_tech_graph` 子图。
- 修改代码后，自动同步更新图谱；不允许代码与图谱不一致。
- 不允许出现逻辑偏差。
