[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py *_test.py benchmark_intent_latency.py
addopts = -ra
markers =
    intent_eval: 真实/ stub 意图评测（默认跳过真实 LLM；见 CHATBI_V2_INTENT_EVAL）
    intent_benchmark: Intent 延迟基准（默认跳过；见 CHATBI_V2_INTENT_BENCH_RUN）

