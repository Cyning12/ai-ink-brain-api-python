# showcase/ — 演示与叙事轨

> **性质**：L2 展示轨 — 人类可读的故事线、演示脚本与叙事稿；**不替代** L0 架构真值 / L1 执行真值。
> **真值优先级**：与 `docs/_tech_graph/`（L0）或 `docs/tasks/` / `docs/harness/reviews/`（L1）矛盾时，**以 L1 为准**。
> **Agent 纪律**：日常不遍历 showcase；仅在 task / 用户显式 `@` 时打开最小文件集。

---

## 子目录索引

| 子目录 | 说明 | 真值链 |
|--------|------|--------|
| [`chatbi-graph-harness-showcase/`](./chatbi-graph-harness-showcase/) | ChatBI Graph · Harness 展示系列：基线合并闸 → P0 基础 → 跨切面 → P1 → 路线图 → Portfolio | L1 证据见 `docs/tasks/done/` + `docs/harness/reviews/` |

---

## 扩展预留

未来新增 showcase 子目录时，按同一格式在本表追加一行：

```markdown
| [`<slug>/`](./<slug>/) | 一句话说明 | L1 证据见 `docs/tasks/done/` |
```
