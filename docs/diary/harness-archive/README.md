# Harness 历史归档（非必读）

> **性质**：`docs/diary/` 易过时产物；Agent **默认不读**（见 `.cursor/rules/08-docs-diary.mdc`）。  
> **用途**：从 `docs/harness/` 瘦身为「最小 harness」时迁出的文件，仅供 Git/人工回溯。

## 目录

| 子目录 | 内容 |
|--------|------|
| `invokes/` | 约 50 个 `invoke_*.md`（2026-05 图谱/闸口/ChatBI Harness 快照） |
| `prompts-removed/` | 22/50 帽与全量复检模板 |
| `planning/` | `HARNESS_V2_P0_ACCEPTANCE`、`VERIFICATION_CI_PATTERN`、`README.workspace-upstream`、`README.harness-spec` |
| `acceptance/` | 闸口 C″ 一次性验收清单 |

## 日常真值

见 **`docs/harness/README.md`**（最小集：prompts 10/20/30/40 + HANDOFF + 新 invoke）。

## 日期

2026-05-22 归档。

## 2026-05-22 注

`22` / `TEMPLATE-task-audit` 已加回 **`docs/harness/prompts/`**；**新** 22 审查只写 **`docs/harness/reviews/`**（本仓专用）。`prompts-removed/` 内 22 副本仅作历史对照。
