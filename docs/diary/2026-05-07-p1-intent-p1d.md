# 2026-05-07 — ChatBI P1-D Intent Prompt / 阈值文档（v1）

关联任务：`docs/tasks/active/task_chatbi_v2_agent_p1d_intent_prompt_and_thresholds_v1.md`  
冻结基线：`docs/diary/2026-05-06-p1-intent-benchmark.md` **复跑五**（`intent_llm_20260507_1529_v1fb1_acc9500_macro9484_tout60.*`，`CHATBI_V2_INTENT_TIMEOUT_S=60`）。

---

## 基线（进入 P1-D 前）

| 指标 | 冻结轮（复跑五） |
|------|------------------|
| macro-F1 | ≈ **0.948**（**回归红线**：本轮改动后不得低于此量级） |
| RAG 24 条 | **22/24**（目标建议 **≥22/24**，争取 **23–24/24**） |
| `v1_fallback` | **1**/60（典型为 **i=16** Text2SQL，超时降级） |
| 真 LLM 误判（非 fallback） | **i=33**（confusion matrix→direct）、**i=56**（多轮「那需要查数据库吗」→direct） |

---

## 本轮代码与配置变更（可审计）

1. **`api/intent_agent.py` — `_llm_decide_v2`**
   - **User prompt**：明确 ChatBI/评测口径；**项目内文档 vs `direct_answer` 通识**；**macro-F1 / confusion matrix / CI / stub / _tech_graph / intent_router** 等默认倾向 **`rag_search`**（与金标一致）。
   - **多轮**：要求结合 `history`；**销售额/Text2SQL 上下文下**「要不要查数据库 / 路由边界」→ **`rag_search`**，避免误判为 direct。
   - **Few-shot**：补「如何计算 confusion matrix」与「那需要查数据库吗」多轮例。
   - **History 上下文**：由最近 **3 条×120 字** 改为最近 **6 条×200 字**，与 `compute_history_hash` 的 6 条窗口对齐，减轻多轮指代丢失。
   - **System**：收紧「仅 JSON、无 Markdown」。
2. **`docs/meta/PROJECT_CONFIG_AI_INK_BRAIN_API_PYTHON.md`**
   - `CHATBI_V2_INTENT_TIMEOUT_S`：补充超时→`v1_fallback` 行为与冻结评测引用。
   - 新增 **`INTENT_MIN_CONFIDENCE`**（`api/agent.py` 读取，默认 `0.6`）。
3. **阈值代码**：本轮**未**修改 `INTENT_MIN_CONFIDENCE` 默认值与 `_fallback_tool_by_low_confidence` 映射；**未**为刷分上调全局超时（仍以环境变量与任务冻结 `60s` 评测为准）。

---

## 前后指标对照（真实 LLM 已跑：`2026-05-07`）

命令要点：`CHATBI_V2_INTENT_TIMEOUT_S=60`、`CHATBI_V2_INTENT_EVAL_OUT=tests/_out/intent_llm_20260507_160444_p1d_live.jsonl`，pytest `-m intent_eval -s`；完整终端日志见同 stem **`.log`**（`tee`）。

| 项 | 冻结基线（复跑五） | P1-D 后（`intent_llm_20260507_160444_p1d_live.*`） |
|----|--------------------|--------------------------------------------------|
| 归档 | `intent_llm_20260507_1529_v1fb1_acc9500_macro9484_tout60.*` | `tests/_out/intent_llm_20260507_160444_p1d_live.{jsonl,csv,.log}` |
| ok / acc | 57/60，≈0.950 | **58/60**，**0.967** |
| macro-F1 | ≈0.948 | **≈0.966**（高于红线） |
| Text2SQL 20 | 19/20 | **20/20** |
| RAG 24 | 22/24 | **22/24**（**持平**） |
| Direct 16 | 16/16 | **16/16** |
| `v1_fallback`（`raw_response.used`） | 1 | **3**（**上升**：稳定性需单独看；非「仅 acc」可掩盖） |
| 真 LLM 误判（非 fallback） | i=33、i=56 | **0**（两条已对齐金标） |
| 仍失败 | i=16（T2S 超时）等 | **`ok=false` 仅 i=36、i=38**：均为 **`v1_fallback`**，`latency_ms`≈65s，`pred=direct_answer`≠`rag_search` |

### `v1_fallback` 三条明细（JSONL）

| i | expected | 结果 | latency_ms 粗值 |
|---|----------|------|-----------------|
| 36 | rag_search | **误判**（超时→V1→direct） | ≈65268 |
| 38 | rag_search | **误判**（同上） | ≈65056 |
| 43 | direct_answer | **标为 ok**（超时→V1 恰为 direct） | ≈60599 |

**结论摘要**：Prompt 侧已修复基线中 **i=33、i=56** 的真 LLM 边界问题；**RAG 桶仍为 22/24**，剩余缺口与 **超时 / 上游 RTT** 强相关（两条 RAG 顶满约 60s+ 后落入 `v1_fallback`）。可选归档改名（在 `tests/_out/` 手工重命名）时可在 stem 中编入 `acc0967_macro0966_v1fb3` 等便于检索的片段。

---

## 复跑命令（合 PR 前必跑）

与 `PROJECT_CONFIG`、`2026-05-06` diary 一致；**导出路径请带时间戳**，避免覆盖 `tests/_out/` 已有归档：

```bash
cd ai-ink-brain-api-python
export CHATBI_V2_INTENT_EVAL=true
export CHATBI_V2_INTENT_LLM=true
export CHATBI_V2_INTENT_TIMEOUT_S=60
export SILICONFLOW_API_KEY="…"
# 示例：export CHATBI_V2_INTENT_EVAL_OUT="tests/_out/intent_llm_20260507_HHMM_p1d_v1.jsonl"
python -m pytest tests/test_intent_agent_accuracy.py -m intent_eval -s
```

**回归（避免 `.env` 中 `CHATBI_V2_INTENT_EVAL=true` 导致全量 pytest 误跑 60 条外呼）**：

```bash
python -m pytest tests/test_intent_agent_accuracy.py -k "not smoke" tests/test_intent_cache.py -q
```

---

## 备注

- 契约：`docs/_tech_graph/_contract_manifest.json` 未改；已执行 `python tools/tech_graph_contract_check.py` → **OK**。
- 若 P1-D 后 **i=16** 仍长时间顶满 `latency_ms` 后 `v1_fallback`，归因以 **上游 RTT/超时** 为主，与 Prompt 分列记录。
