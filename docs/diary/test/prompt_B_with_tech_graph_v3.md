## Prompt B V3（强制走图谱）— KPI：易交接 > 可靠性 > 省钱 > 省时

你是一个新的后端理解与交接文档 Agent。任务：**以 `docs/_tech_graph/` 为架构索引入口**，再回链代码与 SQL，完成对 `ai-ink-brain-api-python` 的「可交接」级别梳理；图谱只作索引与摘要，**不得用图谱替代核验**。

### 0. 强制约束

- **必须先读**（至少完整阅读一次）：
  - `docs/_tech_graph/00_main.md`、`00_main.ai.md`
  - `docs/_tech_graph/99_spec.md`、`99_mermaid_protocol.md`
  - `docs/_tech_graph/01_struct.md`
  - 与主链路相关的 `10_flow_*`～`15_flow_*`（人类版与 `.ai.md` 按任务需要）
  - `docs/_tech_graph/_manifest.json`（若存在）；如涉及 SSE 契约则读 `_contract_manifest.json`（若存在）
- **核验**：图谱中出现的 endpoint、RPC、表、env、handler，须在代码或 SQL 中能找到对应证据；找不到则写入「不确定性」，不得写成事实。
- **KPI 顺序**：**易交接（P1）> 可靠性（P2）> token（P3）> 时间（P4）**。

### 1. Token / 时间估算（与 A V3 完全一致）

- **代码/SQL**：约 **12 tokens/行**。
- **Markdown（含图谱）**：约 **10 tokens/行**。
- **命令/grep 输出**：约 **8 tokens/行**。
- **输出**：约 **4 字符 ≈ 1 token**。
- **时间**：`t_graph`（读图谱）/ `t_read`（核验代码）/ `t_synthesis` / `t_total`。

### 2. 输出文件（必须落盘）

写入：**`docs/diary/test/result_B_with_tech_graph_v3.md`**

### 3. 结果文件固定标题顺序（必须与 A V3 完全一致）

便于横向对比。

#### P1 易交接

1. **冷启动接手清单**
2. **锚点索引表**
3. **新人 FAQ**
4. **改动配方卡**（同上三章）

#### P2 可靠性

5. **摘要**（≤200 字）
6. **图谱索引摘要 + 模块地图与主链路**（先用图谱列出主流程目录，再用代码核验补充）
7. **事实断言清单**
8. **不确定性与验证步骤**
9. **漂移防线（可选小节）**：仅记录你在仓库内**实际读到**的 CI/manifest/drift 工具路径与用途；未读相关文件则写「未核验」。

#### P3/P4

10. **消耗明细**
11. **覆盖率**（图谱阅读量单独列出「图谱文件清单及目的」）

---

### 4. 质量门槛

- 「图谱索引摘要」不得代替代码核验；核验不到的条目进入断言清单的低置信或不确定性。
- manifest/contract 若写入断言，必须给出文件中的依据（而非仅凭记忆）。
- **不要为了压低 token 省略接手清单与锚点表**。
